export const token = {
  KEY: process.env.TOKEN_KEY || "FXFD7tk9dd40",
  TIME: process.env.TOKEN_TIME || "1d",
  REFRESH: process.env.TOKEN_REFRESH || false,
};

export const secretcms = {
  key: process.env.SECRET_CMS_KEY || 10,
};
